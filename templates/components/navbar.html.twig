<nav class="container navbar navbar-collapse navbar-light navbar-blog">
    <h2 class="nav-item">
        <a href="{{ path('app') }}" class="nav-link">Blog.</a>
    </h2>
    <div class="navbar-expand-lg navbar-expand-md navbar-expand-sm">
        <ul class="navbar-nav navbar-blog__fd-row">
            <li class="nav-item ">
                <a class="nav-link" href="{{ path('about') }}">О нас</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path('admin') }}">Админка</a>
                </li>
            {% endif %}
            {% if app.user is null %}
                <li class="nav-item ">
                    <a class="nav-link" href="{{ path('app_login') }}">Войти</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <div class="dropdown navbar-blog__dropdown-item">
                    <span class="nav-item">
                        <a class="nav-link">{{ app.user.firstName }}</a>
                    </span>
                        <div class="dropdown-content">
                            <a>{{ app.user.lastName ~ ' ' ~ app.user.firstName }}</a>
                            <a>{{ app.user.email }}</a>
                            <a href="{{ path('app_logout') }}" class="p-0 pb-1">
                                <button class="btn btn-danger navbar-blog__btn-logout p-0" type="submit">Выйти</button>
                            </a>
                        </div>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>